machine:
  node:
    version: 4.1.0
  ruby:
    version: 2.0.0-p645
dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install ruby-sass
    - script/ci/prepare.sh
  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps

test:
  override:
    - script/ci/tests.sh
