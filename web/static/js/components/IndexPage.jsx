import React from 'react'
import { Router, Route, Link } from 'react-router'

export default React.createClass({
  render: function () {
    return (
      <div>
        Home
      </div>
    );
  }
});
